<UserControl
    xmlns="https://github.com/avaloniaui" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    x:Class="SpectabisNext.Pages.FirstTimeWizard"
    >

    <Grid>
        <!-- <Grid x:Name="SelectEmulatorDirectory">
        </Grid> -->

        <Grid x:Name="Finish" RowDefinitions="*,*" Margin="200" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Grid.Row="0" FontFamily="Roboto:Light" FontSize="24" TextAlignment="Center" Foreground="White">That's it!</TextBlock>
                <Button Grid.Row="1" Name="firstButton" Margin="25">To Library</Button>
        </Grid>
    </Grid>

</UserControl>